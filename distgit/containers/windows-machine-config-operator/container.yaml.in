---
image_build_method: imagebuilder
compose:
  pulp_repos: true

platforms:
  only: x86_64

remote_source:
        repo: https://github.com/openshift/windows-machine-config-operator.git
        ref: ${CI_UPSTREAM_COMMIT}
        pkg_managers: [gomod, git-submodule]
