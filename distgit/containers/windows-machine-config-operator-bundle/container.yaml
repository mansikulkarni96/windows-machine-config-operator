---
operator_manifests:
  manifests_dir: manifests
  repo_replacements:
  - registry: registry-proxy.engineering.redhat.com
    package_mappings:
      windows-machine-config-operator-container: openshift4/windows-machine-config-rhel8-operator
platforms:
  only:
  - x86_64
